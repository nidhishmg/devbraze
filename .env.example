# API base URL for registration backend
VITE_API_URL=http://localhost:4000

# Optional: Google reCAPTCHA site key (v3 or v2 Invisible)
# If provided, the Register form will request a token and include it in the payload as `recaptchaToken`
VITE_RECAPTCHA_SITE_KEY=
