# Server environment
# If set, export endpoints require this key via ?adminKey=... or header x-admin-key
ADMIN_KEY=

# Choose where to store registrations: filesystem | supabase | both
STORAGE=filesystem

# Filesystem output directory (optional)
# DATA_DIR=

# Supabase (only if STORAGE includes 'supabase')
SUPABASE_URL=
SUPABASE_KEY=
# Table name with columns at least: slug, timestamp, name, email, phone, branch, year, notes, extras (json/text)
SUPABASE_TABLE=registrations
